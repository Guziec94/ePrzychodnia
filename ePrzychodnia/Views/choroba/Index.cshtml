@model IEnumerable<ePrzychodnia.Models.choroba>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nazwa_choroby)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.objawy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.diagnoza)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nazwa_choroby)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.objawy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.diagnoza)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_choroba }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_choroba }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_choroba })
        </td>
    </tr>
}

</table>
